package job.Knowledge.map;

public class Set {

    /**
     * Java中的集合包括三大类，它们是Set、List和Map，
     * 它们都处于java.util包中，Set、List和Map都是接口，
     * 它们有各自的实现类。
     * Set的实现类主要有HashSet和TreeSet，
     * List的实现类主要有ArrayList，
     * Map的实现类主要有HashMap和TreeMap。
     * 　　Set中的对象不按特定方式排序，并且没有重复对象。
     * 但它的有些实现类能对集合中的对象按特定方式排序，
     * 例如TreeSet类，它可以按照默认排序，
     * 也可以通过实现java.util.Comparator<Type>接口来自定义排序方式。
     * 　　List中的对象按照索引位置排序，可以有重复对象，
     * 允许按照对象在集合中的索引位置检索对象，
     * 如通过list.get(i)方式来获得List集合中的元素。
     * 　　Map中的每一个元素包含一个键对象和值对象，它们成对出现。
     * 键对象不能重复，值对象可以重复。
     *
     * */
}
